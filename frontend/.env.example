# ===========================================
# CONFIGURAÇÃO DO FRONTEND CRM
# ===========================================

# URL do Backend API (porta padrão: 8081)
# Esta é a URL principal para todas as chamadas de API
VITE_API_URL=http://localhost:8081

# URL do Cliente Encore (deve ser igual à VITE_API_URL)
# Usado pelo cliente gerado automaticamente
VITE_CLIENT_TARGET=http://localhost:8081

# URL do Frontend (porta padrão: 8081) - Unificado na porta 8081
# Usado para CORS e redirecionamentos
VITE_FRONTEND_URL=http://localhost:8081

# Usar autenticação mock para desenvolvimento (true/false)
# true = usa dados mockados, false = conecta com backend real
VITE_USE_MOCK_AUTH=false

# ===========================================
# NOTAS IMPORTANTES:
# - Backend roda na porta 3001
# - Frontend roda na porta 5173
# - Todas as URLs devem usar http://localhost
# - Não usar URLs hardcoded no código
# ===========================================